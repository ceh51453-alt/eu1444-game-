/**
 * UI của cửa từ truyện vào minigame.
 *
 * Chỉ có một thứ: tấm thẻ mời. Ba màn hình minigame đã có sẵn ở `/src/ui/duel`,
 * `/src/ui/battle` và `/src/ui/siege` — cửa này không dựng màn hình mới, nó chỉ
 * quyết định lúc nào một trong ba cái ấy mở ra.
 */

export { EncounterCard, type EncounterCardProps } from './EncounterCard';
